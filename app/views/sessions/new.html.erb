<div class="row">
  <div class="span10 offset1">
    <% if logged_in? %>
      <a href="<%= logout_path %>" class="btn"><%= t('auth.logout') %></a>
    <% else %>
      <div>
        <a href="/auth/github" class="btn"><span><%= t('auth.github_html') %></span></a>
      </div>
    <% end %>
  </div>
</div>
